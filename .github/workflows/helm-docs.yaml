name: Helm Docs

on:
  push:
    branches:
      - master

jobs:
  install-helm-docs:
    runs-on: ubuntu-latest
    steps:
    - name: Install Helm Docs
      uses: stefanprodan/helm-docs@v1.5.0
      with:
        # Helm Docs Version to install (Mandatory)
        helm_docs_version: 1.5.0

