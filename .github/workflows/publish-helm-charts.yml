name: Publish Helm Charts to GitHub Pages

on:
  push:
    branches:
      - feat/github-action

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Important for pushing to gh-pages

      - name: Set up Helm
        uses: azure/setup-helm@v3

      - name: Package Helm charts
        run: |
          mkdir -p packages
          # Package all charts in the 'charts' directory into the packages folder
          helm package charts/* -d packages
          # Create the Helm repo index using your custom domain URL
          helm repo index packages --url https://charts.clouddrove.com/

      - name: Add Artifact Hub metadata file if changed
        run: |
          if [ -f ".artifacthub-repo.yml" ]; then
            if [ ! -f "packages/.artifacthub-repo.yml" ] || ! cmp -s .artifacthub-repo.yml packages/.artifacthub-repo.yml; then
              cp .artifacthub-repo.yml packages/
            fi
          fi

      - name: Add CNAME file for GitHub Pages if changed
        run: |
          echo "charts.clouddrove.com" > temp_cname
          if [ ! -f "packages/CNAME" ] || ! cmp -s temp_cname packages/CNAME; then
            mv temp_cname packages/CNAME
          else
            rm temp_cname
          fi

      - name: Deploy to gh-pages
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          # Fetch the gh-pages branch
          git fetch origin gh-pages || git checkout --orphan gh-pages
          git worktree add gh-pages-temp gh-pages

          # Clean old files and copy new chart packages, index, CNAME, and Artifact Hub metadata
          rm -rf gh-pages-temp/*
          cp -r packages/. gh-pages-temp/   # Copy all files including hidden files

          cd gh-pages-temp
          git add .
          git commit -m "Publish Helm charts from feat/github-action [skip ci]" || echo "No changes to commit"
          git push origin gh-pages
