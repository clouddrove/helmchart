# Add one entry per teammate.
# `name` must be DNS-compatible (lowercase alphanumerics + '-' allowed).
members: []
  # - name: clouddrove
  #   namespace: clouddrove
  #   role: admin            # one of: view, edit, admin
  #   createNamespace: true
  # - name: devops
  #   namespace: devops
  #   role: edit            # one of: view, edit, admin
  #   createNamespace: true