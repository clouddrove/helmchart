apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "changeme.fullname" . }}-test-connection"
  labels:
    {{- include "changeme.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "changeme.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
